package in.amjadit;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.ConfigurableApplicationContext;

import in.amjadit.Entity.Account;
import in.amjadit.Entity.AccountPK;

import in.amjadit.repository.AccountRepository;

@SpringBootApplication
public class Application {

	public static void main(String[] args) {
		ConfigurableApplicationContext context = SpringApplication.run(Application.class, args);

		AccountRepository repository = context.getBean(AccountRepository.class);
		
		AccountPK pk = new AccountPK();
		pk.setAccId(01);
		pk.setAccType("Saving");
		pk.setAccNum(129432712489l);
		
		Account a = new Account();
		a.setHolderName("Amjad");
		a.setBranch("Dildarnagar");
		a.setAccountPk(pk);

		System.out.println("Record is Inserted...");
	}

}
